const mongoose = require('mongoose');
const userModel = require('../../models/user-model');

/**
 *
 * @param {mongoose.connection} connection
 * @param {userModel} user
 */
exports.saveUser = function (connection, user) {

}